services:

  # ─── Backend ────────────────────────────────────────────────
  backend:
    container_name: internal-audit-backend
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
      args:
        HTTP_PROXY: http://103.51.60.125:3128
        HTTPS_PROXY: http://103.51.60.125:3128

    env_file:
      - .env

    environment:
      NODE_ENV: production
      PORT: ${PORT:-3001}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGINS: ${FRONTEND_URL}

      CLICKHOUSE_HOST: ${CLICKHOUSE_HOST}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD}
      CLICKHOUSE_DATABASE: ${CLICKHOUSE_DATABASE}

      HTTP_PROXY: http://103.51.60.125:3128
      HTTPS_PROXY: http://103.51.60.125:3128

    ports:
      - "3001:3001"

    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:3001/health', r => process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

    restart: unless-stopped


  # ─── Frontend ───────────────────────────────────────────────
  frontend:
    container_name: internal-audit-frontend
    build:
      context: .
      dockerfile: apps/nextn/Dockerfile
      args:
        HTTP_PROXY: http://103.51.60.125:3128
        HTTPS_PROXY: http://103.51.60.125:3128

    env_file:
      - .env

    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${BACKEND_URL}
      JWT_SECRET: ${JWT_SECRET}

      HTTP_PROXY: http://103.51.60.125:3128
      HTTPS_PROXY: http://103.51.60.125:3128

    ports:
      - "9002:9002"

    depends_on:
      backend:
        condition: service_healthy

    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:9002', r => process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    restart: unless-stopped