# ─────────────────────────────────────────────────────────────────────────────
# Python runner хамаарал — 2026-09-15-нд аудит хийж шинэчилсэн.
#
# Доод хязгаар = pip-audit-аар эмзэг сан 0 гэж шалгаж, main.py-г smoke test-ээр
# туршсан хувилбар. Дээд хязгаар = дараагийн MAJOR хувилбар (эвдэх өөрчлөлт
# гэнэт орж ирэхээс сэргийлнэ). Major шинэчлэлтийг гараар, туршиж хийнэ.
#
# ⚠️ DB-д хадгалагдсан тайлангийн Python код (python_api_tools.pythonCode) эдгээр
#    сангуудыг шууд ашиглаж болно — pandas 2→3 гэх мэт major шилжилтийн дараа
#    тайлан бүрийг /admin/reports-оос урьдчилан харж шалгана.
# ─────────────────────────────────────────────────────────────────────────────

# FastAPI сервис
fastapi>=0.141.1,<1
uvicorn[standard]>=0.53.0,<1
python-dotenv>=1.2.3,<2

# ClickHouse
clickhouse-connect>=1.8.0,<2
clickhouse-driver>=0.2.11,<0.3

# Excel / Data
pandas>=3.0.5,<4
numpy>=2.5.3,<3
openpyxl>=3.1.5,<4
xlsxwriter>=3.2.9,<4
xlrd>=2.0.2,<3
pyarrow>=25.0.1

# Тооцоо / ML (DB-д хадгалагдсан тайлангийн код ашиглаж болно)
scipy>=1.18.1,<2
scikit-learn>=1.9.1,<2
statsmodels>=0.15.0,<0.16
matplotlib>=3.11.2,<4
seaborn>=0.13.2,<0.14
plotly>=7.0.0,<8
xgboost>=3.4.1,<4
lightgbm>=4.7.0,<5

# Хэрэгслүүд
tqdm>=4.70.1,<5
loguru>=0.7.3,<0.8
requests>=2.34.2,<3
sqlalchemy>=2.0.53,<3

# Oracle — python-oracledb (cx_Oracle-ийн залгамжлагч; cx_Oracle deprecated,
# Python 3.13-д суухгүй). Он-сараар хувилбарладаг тул дээд хязгааргүй.
oracledb>=26.0.0
