FROM node:20-alpine

RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY package*.json ./
RUN npm install

# Source code is mounted via volume at runtime
EXPOSE 3001

CMD ["npm", "run", "start:dev"]
