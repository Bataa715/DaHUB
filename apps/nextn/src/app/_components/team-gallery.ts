/**
 * Нүүр хуудсын хамт олны landscape галерей — төрөл.
 * Зургийн жагсаалтыг `/api/team-gallery` (public/team) өгнө.
 */
export type TeamGallerySlide = {
  id: string;
  src: string;
  alt: string;
};
