# Frontend local dev — Docker ашиглахгүй үед
# Docker ашиглах үед: root-ийн .env файл => docker-compose.dev.yml

# Backend API хаяг (browser-с шууд холбогдох)
NEXT_PUBLIC_API_URL=http://localhost:3001

# ClickHouse Play UI — хэрэглэгчдэд харуулах холбоос
NEXT_PUBLIC_CLICKHOUSE_PLAY_URL=http://localhost:8123/play

# JWT токен шалгах — backend-тай ЯГАНД АДИЛ байх ёстой
JWT_SECRET=CHANGE_THIS_TO_MATCH_BACKEND_JWT_SECRET
